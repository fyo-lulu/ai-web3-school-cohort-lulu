token：模型处理基本单位，取决于tokenizer切分后片段
embedding：输入映射为向量。
transformer：根据向量建立关系
hallucination：模型幻觉，靠外部校验。
multimodel：多模态，输入内容转向量。

### 交易解释器测试
> 结论：这笔交易不是转账，也不是 swap，而是一次 ERC-20 授权（approve）。
> 用户把 COMPASS 这个代币，对某个 spender 地址授予了 无限额度 的使用权限。
> 1. 用户发起了什么动作
> 链上明确可读动作：
> 调用了合约 0xa7a5abc469c345d23246e696bbe2b52df3d91110
> 调用方法 selector 是 0x095ea7b3
> 这对应标准 ERC-20 方法：
> 解码结果：
> spender = 0x80a64c6d7f12c47b7c66c5b4e20e72bc1fcd5d9e
> amount = 0xffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffffff
> 也就是：
> 用户给 0x80a6...5d9e 这个地址，授权其可支配自己钱包中的 COMPASS 代币，额度为 uint256 最大值，通常可称为“无限授权”。
> 2. 涉及哪些资产和地址
> 地址
> 发起人 / owner
> 0x7f9d01fde77f0775244402587bdfa3ef2b550da8
> 被调用的代币合约
> 0xa7a5abc469c345d23246e696bbe2b52df3d91110
> 被授权的 spender
> 0x80a64c6d7f12c47b7c66c5b4e20e72bc1fcd5d9e
> 资产
> 我用链上 eth_call 读了这个合约的标准 ERC-20 元数据，结果是：
> name() → Compass
> symbol() → COMPASS
> decimals() → 18
> 所以这笔交易涉及的资产是：
> COMPASS（18 位精度）
> 3. 事件日志说明了什么
> receipt 里只有 1 条日志，地址也是该代币合约：
> topic0 = 0x8c5be1e5...这是 ERC-20 标准事件：
> 日志解码后：
> owner = 0x7f9d01fde77f0775244402587bdfa3ef2b550da8
> spender = 0x80a64c6d7f12c47b7c66c5b4e20e72bc1fcd5d9e
> value = 2^256 - 1
> 这和 input data 完全一致，说明：
> 这笔交易成功执行了 approve，链上状态已更新。
> 4. 交易层面的补充信息
> 基本情况
> 链：Ethereum Mainnet
> 交易状态：成功（status = 1）
> ETH 转账金额：value = 0
> Gas Used：0xc10a = 49,418
> 交易类型：EIP-1559 type 2
> 时间
> 交易对象里有：
> blockNumber = 0x17f589f
> blockTimestamp = 0x6a0b2f7b
> 这个时间来自链上区块头，不是页面文案。
> 5. 哪些信息来自链上数据，哪些是模型推断
> 直接来自链上数据
> 这些是确定的：
> 发起地址
> 0x7f9d01fde77f0775244402587bdfa3ef2b550da8
> 目标合约地址
> 0xa7a5abc469c345d23246e696bbe2b52df3d91110
> 方法 selector
> 0x095ea7b3
> 该 selector 对应 ERC-20 approve
> 这是标准 ABI 常识，可直接解码
> spender 地址
> 0x80a64c6d7f12c47b7c66c5b4e20e72bc1fcd5d9e
> 授权额度
> uint256 max
> 即 0xffff...ffff
> 事件日志
> 是标准 Approval(owner, spender, value)
> 代币元数据
> name = Compass
> symbol = COMPASS
> decimals = 18
> 这是通过链上 eth_call 读取标准方法得到的
> 模型推断 / 解释性判断
> 这些是合理推断，但不是链上直接写明的：
> 这是“无限授权”
> 因为 amount 是 uint256 最大值，行业里通常这样称呼
> 用户可能是为了后续 dApp 操作做准备
> 比如 swap、质押、deposit、claim 前常先 approve
> 但这笔交易本身没有告诉我们具体下一步要做什么
> 0x80a6...5d9e 可能是某协议合约 / 路由器 / 中间执行合约
> 这是常见场景
> 但如果不额外查标签、源码、前后交易，就不能下结论
> 6. 模型不确定的地方
> 这里要说清楚，不然容易装懂。
> 不确定 1：spender 到底是谁
> 我这次没有拿到 Etherscan API 的合约源码/标签数据，因为它需要有效 API key。
> 所以目前不能确定 0x80a64c6d7f12c47b7c66c5b4e20e72bc1fcd5d9e 具体属于：
> 某个 DEX router
> 某个聚合器
> 某个钓鱼合约
> 某个代理合约
> 某个普通 EOA / 未验证合约
> 也就是说：
> 我能确定“你授权给了谁”，但不能只靠这笔交易本身确定“这个谁到底安全不安全、属于哪个产品”。
> 不确定 2：用户真实意图
> 从链上只能看出：
> 用户进行了 approve
> 但不能 100% 确定用户是为了：
> swap
> staking
> claim
> bridge
> NFT mint 前授权
> 还是误签 / 钓鱼签名
> 不确定 3：ABI 的“相关合约 ABI”
> 我能确定用到的 ABI 片段，但不能说我已经拿到了完整 verified ABI。
> 
> 当前可确定的最小 ABI 片段是：
> 这是标准 ERC-20 ABI 片段，足够解释这笔交易；
> 但不是我从 Etherscan verified source 成功拉下来的完整 ABI。
> 7. 用人话解释这笔交易
> 可以直接写成这样：
> 这笔交易本身没有转出你的 COMPASS，也没有把 ETH 打给别人。
> 它做的是：允许 0x80a64c6d7f12c47b7c66c5b4e20e72bc1fcd5d9e 这个地址，今后可以代你使用 COMPASS。
> 而且授权额度是无限大。
> 所以真正的风险不在这笔交易“当下转出了什么”，而在于以后这个 spender 是否能调用 transferFrom 把你的 COMPASS 转走。
> 8. 如果要签类似交易，用户应该检查什么
> 这是最关键的部分。
> 必查 1：你在授权给谁
> 看清楚 spender 地址，不是只看前端名字。
> 
> 要确认：
> 这个地址是不是你原本要交互的协议官方地址
> 官网 / 官方文档 / 官方 X / GitHub / DefiLlama / Etherscan 标签能不能对上
> 是不是被假前端换成了陌生地址
> 必查 2：授权的是哪种资产
> 很多人只看“Approve”，不看 token。
